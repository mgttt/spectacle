Precompiled 1.2 UI resources (MIT, from the last official Spectacle.app).

`ibtool` / `actool` are not in Command Line Tools, so the CLT Makefile copies these nibs, `Assets.car`, and `Spectacle.icns` instead of compiling the XIBs and asset catalog.
